create table book
(
    id          bigint generated by default as identity
        constraint book_pk
            primary key,
    title       varchar(255)             not null,
    description varchar(255)             not null,
    status      varchar(255)             not null,
    status_desc varchar(255)             not null,
    created_at  timestamp with time zone not null,
    updated_at  timestamp with time zone,
    isbn        varchar(255)             not null
        constraint book_pk_2
            unique
);

comment on table book is 'Book table for notification app';

comment on column book.title is 'book title';

comment on column book.description is 'book description';

comment on column book.status_desc is 'Status description, but It mean to remark when approve processing';

comment on column book.created_at is 'timestamp created moment ';

comment on column book.updated_at is 'Modified timestamp event';

comment on column book.isbn is 'ISBN unique number';

alter table book
    owner to demo;

